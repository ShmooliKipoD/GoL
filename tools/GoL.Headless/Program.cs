// Soak runner. Fills out in Step 4 with --ticks/--seed and the world hash;
// for now it only proves the sim assembly loads with no graphics stack present.
using GoL.Sim;

Console.WriteLine($"{SimInfo.Name} headless runner - no world to run yet (Step 4).");
return 0;
